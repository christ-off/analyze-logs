<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- webapp-mapper: web app mapping / tech-stack analysis tool ("webapp-mapper/0.1 (public technical analysis)").
         Reclassify log rows ingested before this existed. -->
    <changeSet id="018-static-ua-add-webapp-mapper" author="analyze-logs">
        <sql>
            INSERT INTO static_ua (ua_name, ua_group, ua_label, pattern, sort_order) VALUES
            ('webapp-mapper', 'Other Bots', 'webapp-mapper', 'webapp-mapper', 82);
            UPDATE cloudfront_logs SET ua_name = 'webapp-mapper'
            WHERE ua_name = 'Unknown' AND user_agent LIKE '%webapp-mapper%';
        </sql>
    </changeSet>

</databaseChangeLog>
